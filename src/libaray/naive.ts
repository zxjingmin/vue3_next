import {
  // create naive ui
  create,
  // component
  NButton,
  NForm,
  NFormItem,
  NInput,
} from 'naive-ui';

const naive = create({
  components: [NButton, NForm, NFormItem, NInput],
});

export default naive;
